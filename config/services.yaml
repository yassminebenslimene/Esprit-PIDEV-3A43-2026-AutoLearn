# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    uploads_dir: '%kernel.project_dir%/public/uploads'
<<<<<<< HEAD
    
=======
    admin_email: 'noreply@scholar.com'

>>>>>>> fb4a43f494307a186b8da2e3098a2944d2e0ef9f
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            string $adminEmail: '%admin_email%'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
<<<<<<< HEAD
    
    # Configuration du WeatherService avec la clé API
    App\Service\WeatherService:
        arguments:
            $weatherApiKey: '%env(WEATHER_API_KEY)%'

    # Configuration des services AI avec Hugging Face
    App\Service\AIReportService:
        arguments:
            $huggingfaceApiKey: '%env(HUGGINGFACE_API_KEY)%'
            $huggingfaceModel: '%env(HUGGINGFACE_MODEL)%'
    
    App\Service\FeedbackAnalyticsService:
        arguments: []
    
    # Configuration du GrokQuizGeneratorService avec la clé API Groq
    App\Service\GrokQuizGeneratorService:
        arguments:
            $grokApiKey: '%env(GROQ_API_KEY)%'
    
    # Configuration du QuizCorrectorAIService avec la clé API Groq
    App\Service\QuizCorrectorAIService:
        arguments:
            $grokApiKey: '%env(GROQ_API_KEY)%'
    
    # Configuration du QuizTutorAIService avec la clé API Groq
    App\Service\QuizTutorAIService:
        arguments:
            $grokApiKey: '%env(GROQ_API_KEY)%'
    
    # Configuration du ChapterExplainerService avec la clé API Groq
    App\Service\ChapterExplainerService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'
    
    # Configuration du TranslationService avec la clé API Groq
    App\Service\TranslationService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'

    # Configuration Groq Service (Ilef)
    App\Service\GroqService:
        arguments:
            $httpClient: '@http_client'
            $logger: '@logger'
            $groqApiKey: '%env(GROQ_API_KEY)%'
            $groqApiUrl: '%env(GROQ_API_URL)%'
            $groqModel: '%env(GROQ_MODEL)%'

    # Language Detector Service (no arguments needed)
    App\Service\LanguageDetectorService: ~

    # AI Assistant Service (with direct database access, no RAG)
    App\Service\AIAssistantService:
        arguments:
            $groqService: '@App\Service\GroqService'
            $languageDetector: '@App\Service\LanguageDetectorService'
            $actionExecutor: '@App\Service\ActionExecutorService'
            $logger: '@logger'
            $security: '@security.helper'
            $em: '@doctrine.orm.entity_manager'
            $userRepository: '@App\Repository\UserRepository'
            $coursRepository: '@App\Repository\Cours\CoursRepository'
            $evenementRepository: '@App\Repository\EvenementRepository'
            $communauteRepository: '@App\Repository\CommunauteRepository'
            $activityRepository: '@App\Bundle\UserActivityBundle\Repository\UserActivityRepository'
            $challengeRepository: '@App\Repository\ChallengeRepository'
            $quizRepository: '@App\Repository\QuizRepository'
            $chapitreRepository: '@App\Repository\Cours\ChapitreRepository'
            $postRepository: '@App\Repository\PostRepository'
            $commentaireRepository: '@App\Repository\CommentaireRepository'
    
    # Action Executor Service (with all repositories for CRUD operations)
    App\Service\ActionExecutorService:
        arguments:
            $em: '@doctrine.orm.entity_manager'
            $userRepository: '@App\Repository\UserRepository'
            $equipeRepository: '@App\Repository\EquipeRepository'
            $evenementRepository: '@App\Repository\EvenementRepository'
            $coursRepository: '@App\Repository\Cours\CoursRepository'
            $chapitreRepository: '@App\Repository\Cours\ChapitreRepository'
            $challengeRepository: '@App\Repository\ChallengeRepository'
            $communauteRepository: '@App\Repository\CommunauteRepository'
            $postRepository: '@App\Repository\PostRepository'
            $commentaireRepository: '@App\Repository\CommentaireRepository'
            $quizRepository: '@App\Repository\QuizRepository'
            $logger: '@logger'

    # Workflow service alias for Evenement
    Symfony\Component\Workflow\WorkflowInterface $evenementPublishingStateMachine: '@state_machine.evenement_publishing'

    # Configuration des services AI de Baha (Community features)
    App\Service\AiReactionService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'
            $groqApiUrl: '%env(GROQ_API_URL)%'
            $groqModel: '%env(GROQ_MODEL)%'
    
    App\Service\SentimentAnalysisService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'
            $groqApiUrl: '%env(GROQ_API_URL)%'
            $groqModel: '%env(GROQ_MODEL)%'
    
    App\Service\TitleSuggestionService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'
            $groqApiUrl: '%env(GROQ_API_URL)%'
            $groqModel: '%env(GROQ_MODEL)%'

    App\Service\AiSummaryService:
        arguments:
            $groqApiKey: '%env(GROQ_API_KEY)%'
=======
    App\Service\EmailService:
        arguments:
            $adminEmail: '%admin_email%'
>>>>>>> fb4a43f494307a186b8da2e3098a2944d2e0ef9f
