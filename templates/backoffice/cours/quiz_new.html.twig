{% extends 'backoffice/base.html.twig' %}

{% block title %}Nouveau Quiz{% endblock %}
{% block page_title %}Créer un nouveau quiz{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div style="background: rgba(34, 197, 94, 0.2); color: #22c55e; padding: 12px 20px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #22c55e;">
            {{ message }}
        </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div style="background: rgba(239, 68, 68, 0.2); color: #ef4444; padding: 12px 20px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #ef4444;">
            {{ message }}
        </div>
    {% endfor %}

    <section class="content-grid">
        <div class="glass-card form-card">
            <div class="card-header">
                <div>
                    <h2 class="card-title">Nouveau Quiz</h2>
                    <p class="card-subtitle">Chapitre: {{ chapitre.titre }} - Cours: {{ cours.titre }}</p>
                </div>
            </div>
            <div class="card-body">
                {{ form_start(form, {'attr': {'class': 'custom-form'}}) }}
                    {{ form_errors(form) }}
                    
                    <div class="form-group">
                        {{ form_label(form.titre) }}
                        {{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.titre) }}
                        {% if form.titre.vars.help %}
                            <small class="form-help">{{ form.titre.vars.help }}</small>
                        {% endif %}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.description) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.description) }}
                        {% if form.description.vars.help %}
                            <small class="form-help">{{ form.description.vars.help }}</small>
                        {% endif %}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.etat) }}
                        {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.etat) }}
                        {% if form.etat.vars.help %}
                            <small class="form-help">{{ form.etat.vars.help }}</small>
                        {% endif %}
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <svg style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 4px;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/>
                                <polyline points="17 21 17 13 7 13 7 21"/>
                                <polyline points="7 3 7 8 15 8"/>
                            </svg>
                            Créer
                        </button>
                        <a href="{{ path('app_cours_chapitre_quizzes', {'coursId': cours.id, 'chapitreId': chapitre.id}) }}" class="btn btn-secondary">
                            <svg style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 4px;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M19 12H5"/>
                                <path d="M12 19l-7-7 7-7"/>
                            </svg>
                            Annuler
                        </a>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </section>
{% endblock %}
