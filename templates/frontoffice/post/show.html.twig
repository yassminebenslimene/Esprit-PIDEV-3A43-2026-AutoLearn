{% extends 'base_front.html.twig' %}

{% block title %}Post | Scholar{% endblock %}

{% block body %}

<style>
.post-show-container { max-width: 720px; margin: 80px auto; }
.post-show-card {
    background: white;
    border-radius: 14px;
    padding: 24px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.08);
}
.post-show-card .post-media img,
.post-show-card .post-media video {
    max-width: 100%;
    border-radius: 12px;
    margin-top: 12px;
}
.post-show-card .post-meta { color: #666; font-size: 14px; margin-top: 12px; }
.post-actions { margin-top: 20px; display: flex; gap: 12px; flex-wrap: wrap; }
.btn-back {
    background: #f1f3ff;
    color: #333;
    padding: 10px 20px;
    border-radius: 999px;
    text-decoration: none;
}
.btn-edit {
    background: linear-gradient(135deg, #7a6ad8, #6c63ff);
    color: white;
    padding: 10px 20px;
    border-radius: 999px;
    text-decoration: none;
}
</style>

<<<<<<< HEAD
<div class="page-heading header-text">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h3>Post</h3>
                <span class="breadcrumb">
                    <a href="{{ path('app_frontoffice') }}">Home</a>
                    > <a href="{{ path('app_communaute_show', { id: post.communaute.id }) }}">{{ post.communaute.nom }}</a>
                    > Post
                </span>
            </div>
        </div>
    </div>
</div>

<div class="post-show-container">
    <div class="post-show-card">
        {% if post.contenu %}
            <p>{{ post.contenu }}</p>
        {% endif %}


        {% if post.imageFile %}
            <div class="post-media">
                <img src="{{ asset('uploads/images/' ~ post.imageFile) }}" alt="Image du post">
            </div>
        {% endif %}

        {% if post.videoFile %}
            <div class="post-media">
                <video controls src="{{ asset('uploads/videos/' ~ post.videoFile) }}"></video>
            </div>
        {% endif %}

        <p class="post-meta">
            Posté le {{ post.createdAt|date('d/m/Y H:i') }}
            dans <a href="{{ path('app_communaute_show', { id: post.communaute.id }) }}">{{ post.communaute.nom }}</a>
        </p>

        <div class="post-actions">
            <a href="{{ path('app_communaute_show', { id: post.communaute.id }) }}" class="btn-back">← Retour à la communauté</a>
            {% if app.user %}
                <a href="{{ path('app_post_edit', { id: post.id }) }}" class="btn-edit">Modifier</a>
                {{ include('frontoffice/post/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>
</div>

=======
    <a href="{{ path('app_post_index') }}">back to list</a>

    <a href="{{ path('app_post_edit', {'id': post.id}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}
>>>>>>> fb4a43f494307a186b8da2e3098a2944d2e0ef9f
{% endblock %}
